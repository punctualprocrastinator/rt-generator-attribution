# Large binaries. The four checkpoints are ~38 MB each and the pretraining corpora
# ~1.5 GB; both are needed to reproduce but neither belongs in git. See the README for
# where to put them.
model_checkpoints/
data/
*.pt
*.tar

# Working directories and archives
unzipped/
workshop_outputs.zip
paper/figs/*.pdf

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.synctex.gz
paper/*.fdb_latexmk
paper/*.fls

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Working notes kept out of the repo; the README is self-contained
MECH_INTERP_EXPERIMENTS.md
NEXT_EXPERIMENTS.md
EXPERIMENT_SHORTLIST.md
EXPERIMENT_SHORTLIST*.md
analysis/RESULTS.md
RFMs.md
SPECULATIVE_ABLATION.md
SELF*.md
trained-attack-policies*.md
